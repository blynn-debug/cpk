# e4a97e8 검증 결과

검토일: 2026-09-16. 대상: `e4a97e85746b697b941ee2adac61fa2167cbb51d`.

## 해결 확인

로컬/맥미니 소스·테스트·fixture 11개 파일의 SHA-256 일치를 확인했다. 기존 유닛 32건과 회귀 24건, `audit_81a7aa1.py`의 기존 16개 시나리오가 모두 통과했다. 수집 카운터 경쟁도 이제 두 실행이 runs_today=2로 보존된다. 기존 재현 항목의 수정은 확인됐다.

아래 경계 시나리오 3개를 기존 audit에 **메모리에서만 추가**해 같은 가짜 HTTP/CDP·임시 HOME 환경에서 실행했다. 앱 소스와 기존 테스트 파일은 수정하지 않았다. 실제 HTTP·소켓 연결, 운영 Chrome 시작/종료, 알림은 차단했다. 전체 결과는 기존 16개 통과 + 추가 3개 실패다. 온라인 테스트 본문은 실행하지 않았다.

## 1. 실접속 테스트는 import 단계부터 실행을 막아야 함

`tests/test_functional.py:23`의 데코레이터가 `_session_alive()`를 평가하고, 그 함수가 관문 밖 `cs.canary()`를 호출한다. 가짜 쿠키통이 존재하고 공통 중단이 설정된 상태에서 모듈을 import하자 **HTTP 1건, 공통 집계 0건**이었다. 그 뒤 테스트 클래스는 skip으로 표시됐다.

따라서 `run_all.sh`의 online/full/all 안내만 바꾸거나 테스트 클래스에 skip을 붙이는 것으로는 충분하지 않다. 직접 `python -m unittest tests.test_functional` 실행과 unittest discovery도 고려해 **프로브 실행 전 모듈 단계에서** 중단해야 한다.

전용 HOME을 사용하는 `test_integration.py`가 운영 control.json·요청 잠금을 공유하지 않는 문제도 남아 있다. 공유 구현 전에는 기능/통합 테스트를 코드에서 비활성화하고 README의 실행 예시를 현재 상태로 맞춘다. 재개하려면 데이터·쿠키 격리와 별개로 같은 외부 IP의 제어 상태/잠금을 공유해야 한다. 문서화는 이 실행 제약을 설명하는 작업이다.

## 2. harvest에서는 403 뒤 예산 소진 시 중단 설정을 건너뜀

위치: `cpk_browser.py:207` 이후 harvest의 try/except.

재현: 예산 2, 홈 방문 1회, 검색 1회에서 HTTP 403. 다음 재시도 관문은 BudgetExceeded를 던진다. 결과는 `blocked=BudgetExceeded`, 페이지 이동/집계 2, **공통 중단 없음**이다.

검색 fetch에는 앞선 실패 보존 처리가 추가됐지만 harvest는 예외가 발생하면 루프 뒤의 HTTP 분류/maybe_pause_on_block 구간을 건너뛴다. 검색 fetch와 같은 방식으로 이미 받은 HTTP 결과를 보존하고, 재시도 허용 여부와 실패/중단 처리를 분리해야 한다.

## 3. 마지막 실패 처리와 공통 중단 기록 사이에 다른 요청이 허가됨

위치: `cpk_browser.py`의 fetch: request_gate를 빠져나온 뒤 classify_outcome → _save_evidence → 최종 maybe_pause_on_block을 실행하는 순서.

재현: 지속 403 응답을 주고 세 번째 실패의 증거 저장 직전에 첫 실행을 잠시 대기시켰다. 이때 다른 실행이 실제 request_gate를 통과했다. 그 뒤 첫 실행을 재개하면 최종 http_error와 공통 중단이 기록됐다.

결과: `other_admitted=True`, `first_outcome=http_error`, `eventually_paused=True`.

이는 **이미 기록된 중단을 무시한 요청**과는 다르다. 최종 차단을 확인한 뒤 중단을 기록하기 전까지의 틈에서 새 요청이 허가되는 문제다. 재시도 정책 자체를 없앨 필요는 없다. 마지막 시도의 결과 분류와 필요한 공통 중단을 다음 요청 허가 전에 완료하도록 직렬화하면 된다. 증거 저장 지연이 이 순서를 깨지 않도록 한다.

## 다음 순서

1. 온라인 기능/통합 테스트의 프로브 이전 실행 차단과 문서 정리.
2. harvest의 부분 실패 보존과 최종 차단 전파 순서 보완.
3. 추가 세 경계 사례를 회귀 테스트에 포함.
4. 기존 운영 요청의 결과를 수동 관측해 직접 연결 가용성과 비용 근거를 확보한 뒤 프록시 필요성 판단.

# 81a7aa1 재검토 — 통과 범위와 남은 결함

검토일: 2026-09-16. 대상: `81a7aa1e208e42d913749c05bcd51792e820c24b`.

## 결론

관문의 중단·예산 판정 원자화와 헬스 갱신 잠금은 **실제 스레드/프로세스 경쟁 테스트도 통과**했다. 다만 전체 진입점의 요청 제어와 수집 카운터 보존은 아직 완료되지 않았다. 아래는 이전 요청 제어·실패 전파·상태 보존 기준을 다시 검증한 결과다.

## 검증 방법

- 로컬/맥미니 소스 6개, 테스트 2개, HTML fixture 1개의 SHA-256 일치를 확인했다.
- 기존 오프라인 테스트: 유닛 32 + 회귀 24 = **56건 통과**, 실패·오류·skip 0건.
- [추가 재현 스크립트](tests/audit_81a7aa1.py): **16개 시나리오 중 10개 통과, 6개 실패**. 아래 네 가지 원인으로 묶인다.
- 실제 HTTP·소켓 연결을 막고 가짜 HTTP/CDP 탭을 사용했다. 임시 상태와 데이터만 사용했다. 운영 Chrome 시작/종료 및 알림은 금지했다.
- 아래 호출 수는 모의 전송/페이지 이동 시도 수다. 쿠팡 실접속·IP 회복·이미 열린 페이지의 비동기 네트워크 활동에 대한 관측치는 아니다.

## 해결 확인

| 시나리오 | 결과 |
|---|---|
| 이미 중단된 상태의 browser search·health·harvest·공용 autocomplete | 페이지 이동/HTTP/집계 0 |
| 예산 0의 위 경로(기본 워밍업 포함) | 페이지 이동/HTTP/집계 0 |
| 구형 cpk_search CLI | 종료 코드 2, HTTP 0 |
| control.lock을 기다리는 중 중단 기록 | 잠금 획득 후 RequestPaused, 집계 0 |
| 스레드 4개가 예산 1을 동시에 경쟁 | 허가 1, 집계 1 |
| 스레드로 헬스 갱신 16회 | runs=16, fails=16 |
| 별도 프로세스 3개가 각각 헬스 갱신 4회 | runs=12, fails=12 |
| 충분한 예산에서 온디맨드 지속 403 | http_error, 공통 중단 설정, 후속 자동완성 HTTP 0 |
| 최초 자동완성 HTTP 403 | ok=False, 호출 1, 축약 안 함 |
| 전날 예산 소진 후 새 날짜의 관문 진입 | 이전 count=0 취급, 새 날짜 집계 1 |

파일 잠금이라는 이유로 동시성 검증을 순차 실행에 한정할 필요는 없다. 위 테스트는 임시 HOME 안의 실제 flock을 사용했고, 프로세스 경쟁도 재현했다.

## P1 — 1. 쿠키 주입 및 재발급의 검증 HTTP가 관문 밖에 남음

근거: `cpk_import.py:23`, `cpk_browser.py:395`, `cpk_session.py`의 canary → search_html → get.

- 중단을 설정하고 `cpk_import.main()`에 가짜 Cookie 헤더를 입력했다. **HTTP 1건이 호출됐고 공통 집계는 0**이었다.
- 정상 브라우저 harvest가 끝난 직후, verify 전에 다른 작업이 중단을 설정하는 상황을 만들었다. `cb.reissue(verify=True)`는 **중단 중 검증 HTTP 1건을 보내고 True를 반환**했다. 집계에는 앞선 브라우저 이동 2건만 남았다.

harvest의 페이지 이동을 감싼 것만으로 재발급 전체 경로를 보호하지 못한다. 유지할 검증은 전송 관문을 거치게 하고, 불필요한 Python 검증은 폐기해야 한다. 중단/예산 거절을 수집 실패나 재발급 성공으로 바꾸지 않고 상위 호출자에게 전달한다.

## P1 — 2. 검색 외 경로가 차단을 공통 중단으로 전파하지 않음

근거: `cpk_browser.py:199`의 harvest, `cpk_session.py:248`의 autocomplete_keywords.

- `harvest`에 지속 403을 주면 **페이지 이동 4회 뒤에도 중단 없음**이었다. 관문은 지나지만 결과의 HTTP 상태/챌린지를 판정해 maybe_pause_on_block을 호출하지 않는다.
- 공용 자동완성에 403을 주면 **HTTP 1회, ok=False, 중단 없음**이었다. 실패 구분은 고쳐졌지만 상위 헬퍼가 HTTP 상태를 버리며 차단 전파는 하지 않는다.
- 홈 워밍업도 관문 진입만 추가됐으며 응답 차단 판정은 하지 않는다(정적 확인).

각 전송의 결과를 공통 정책으로 분류하고, 명확한 차단의 중단 설정까지 다음 요청 허가 전에 처리해야 한다. 503 등 기타 HTTP 오류와 403/429·챌린지는 구분한다. 최초 자동완성 실패뿐 아니라 축약 후보 도중 실패 시 다음 후보를 계속 호출하는지 함께 검증한다.

## P1 — 3. 403 이후 재시도 예산 소진 시 차단 결과가 사라짐

근거: `cpk_browser.py:289`의 예산 예외 즉시 반환과 `:298` 이후에만 실행되는 증거 저장/중단 설정.

예산을 2로 설정했다. 홈 워밍업 1회, 검색 1회가 실행되고 검색은 HTTP 403이었다. 재시도 관문에서 예산이 소진되자 반환값은 **outcome=budget, http_status=None**, 증거 파일 **0개**, 공통 중단 **없음**이었다.

이미 받은 응답과 다음 시도 허용 여부를 분리해야 한다. 실패마다 HTTP 상태·증거·중단 정책을 먼저 처리하고, 이후 재시도 여부를 결정한다. 예산 거절이 앞선 차단 결과를 덮으면 안 된다. 현재 코드에서는 결과 분류/중단 설정이 검색 잠금 밖에서 실행되는 순서도 남아 있다(정적 확인).

## P2 — 4. 수집 상태를 실행 잠금 전에 읽어 카운터가 유실됨

근거: `cpk_collect.py:168`의 read_state, `:212`의 collect.run.lock.

실제 두 실행의 순서를 다음처럼 동기화했다. 네트워크와 수집 본문만 가짜 처리하고 실제 상태 파일·실행 잠금을 사용했다.

1. 실행 B가 이전 상태를 읽고 잠시 대기한다.
2. 실행 A가 잠금을 얻어 수집 실행을 마치고 runs_today=1을 저장한다.
3. B가 진행해 잠금을 얻지만, 앞서 읽은 상태에서 계산한다.

두 실행 모두 종료 코드 0이었지만 최종 **runs_today=1**이었다. 기대값은 2다. health의 update_health 수정과 별개다. 상태 읽기·날짜 초기화·카운터 증가는 실행 잠금 획득 후 수행해야 한다.

## 온라인 통합 테스트는 별도 해결 필요

통합 테스트의 전용 HOME은 control.json과 요청 잠금도 별도로 만든다. 같은 외부 IP로 실접속한다면 운영 중단/예산 공유가 끊기는 것은 문서화만으로 해결되지 않는다. 온라인 실행을 명시적으로 보류하거나 데이터/쿠키 HOME과 별도의 공통 제어 경로를 공유하도록 구현해야 한다. 이번 검토에서는 온라인 테스트를 실행하지 않았다.

## 재현 방법

macOS/Linux의 CPK 코드와 의존성 환경에서:

```sh
.venv/bin/python tests/audit_81a7aa1.py
```

운영 환경 파일을 읽을 필요가 없다. 스크립트는 임시 상태를 만들고 정리한다. 각 행에 case/actual/expected/passed가 나오며, 현재 대상 커밋에서는 마지막에 `cases=16, passed=10, failed=6`과 종료 코드 1이 예상된다. 리뷰 재현용 파일이므로 unittest의 기본 test_* 검색에는 포함되지 않는다. 해당 결함을 수정하면 이 시나리오들을 정규 회귀 테스트로 옮길 수 있다.

# 2026-09-17 단건 실패의 단계별 분석

결론: **쿠팡의 접근 제한 HTML을 받은 것이 직접적인 수집 실패다. CPK는 HTTP 상태를 기록하지 못했고, 그 HTML도 접근 제한으로 인식하지 못해 `load_error`로 표시했다.** IP·세션·접속 경로 중 어떤 조건이 제한을 유발했는지는 이번 증거로 확정할 수 없다.

이번 작업은 저장 증거 분석과 오프라인 재현이다. 쿠팡 추가 요청 0건, 운영 코드 변경·배포 없음. 사용자가 닫은 브라우저를 다시 열지 않았다. 운영 `control.json`의 실행 전후 SHA-256도 동일했다.

## 1. 실제 시도의 조건과 증거

- 2026-09-17 03:36:03~03:36:08 KST, 집 맥미니에서 `2단 선반` 1페이지.
- 실행 전: 공통 중단 없음, 당일 집계 0/300, 최소 간격 20초, Chrome 접속 가능, 정기 수집·헬스·재발급 잡 미등록, `CPK_PROXY` 미설정.
- 기존 Chrome에 새 탭을 만들고 검색 URL로 이동했다. `search_session(warmup=False)` 및 `fetch(tries=1)` 사용.
- **기본 `cb.search()`의 홈 워밍업 및 최대 3회 시도와 조건이 다르다.** 요청을 제한한 단건 관측이며 기본 경로 전체나 UI 검색의 성공률 시험으로 해석하지 않는다. 워밍업 생략의 영향도 미확인이다.
- 실제 집계는 `ondemand_test: 1`. 실패 후 추가 검색·자동완성은 실행하지 않았다. 이 수치는 브라우저 하위 리소스까지 합친 총 HTTP 요청 수가 아니다.
- 결과: `http_status=null`, `error=null`, `product_count=0`, `outcome=load_error`, 이후 중단 0초.

실행 보고서: [단건 결과](state/manual-tests/2026-09-17-one-keyword.json).
원본 증거: 맥미니 `~/cpk/state/evidence/20260917_033608_load_error.json`, SHA-256 `3a9bec85e162d6e847b72fc32d9cf49109cdc60685da6518da84a6668301133d`.
로컬 사본: [실패 증거](state/manual-tests/2026-09-17-failure-evidence.json).

저장 HTML은 3,637자로 저장 상한 20,000자보다 짧고 `</html>`까지 있다. 다음 특징을 함께 확인했다.

- `<div id="error403">`
- 제목 문구: “요청하신 페이지의 사용권한이 없습니다.”
- 본문 문구: “입력하신 페이지주소는 사용권한이 제한된 페이지입니다.”
- 페이지 자체의 분석 태그 제목: `Error 403`.

이는 **접근 제한 안내 템플릿**이라는 증거다. 템플릿 이름만으로 실제 HTTP 상태가 403이었다고 복원하지 않는다. 저장 HTML의 스크립트·이미지를 다시 실행할 필요 없이 텍스트로 분석했다.

## 2. 원자적 분해

| 단계 | 관측 | 판정 |
|---|---|---|
| 회사 → 집 맥미니 접속 | SSH 실행 및 결과 회수 성공 | 이번 실패를 회사 Wi-Fi 연결 실패로 설명할 근거 없음 |
| 요청 허가 | 중단·예산 검사를 통과했고 검색 1건 집계 | 요청 관문이 실행을 막은 것이 아님 |
| 브라우저 조작·DOM 읽기 | 검색 URL, `쿠팡!` 제목, HTML 확보; Python 예외 없음 | 브라우저 명령 전체 실패나 빈 화면으로 볼 수 없음 |
| 검색 콘텐츠 제공 | 상품 대신 접근 제한 안내 문서 | 상품·연관검색어를 얻지 못한 직접 원인 |
| HTTP 상태 기록 | `null`; 원시 CDP 이벤트 기록 없음 | 실제 상태 코드와 누락 시점을 사후 확정할 수 없음 |
| 콘텐츠 판정 | 제한 문구가 있는데 `load_error` | 접근 제한 HTML 인식 누락, 확정 결함 |
| 중단 전파 | 중단 0초 | 잘못된 분류가 공통 중단까지 전달되지 않음 |
| 상품 파싱 | 실제 실행은 `outcome != ok`여서 생략; 증거를 별도 파싱해도 0개 | 이번 HTML에 상품이 없어 파서 선택자 수정으로 해결할 상황이 아님 |
| 자동완성 | 이번 단건 실행 도구가 검색 실패 후 생략 | 자동완성 API의 성공·실패는 이번에 시험하지 않음 |

이 순서는 데이터가 통과하는 단계다. 브라우저 이벤트는 실제로 비동기 전달되므로, 순차적인 명령 응답만 읽는 구현으로는 충분하지 않다.

## 3. 코드에서 재현한 문제

**A. 응답 상태 유실 — [cpk_browser.py](cpk_browser.py)의 `Tab.call`, `Tab.goto`, `Tab.eval`.**

`call()`은 기다리는 명령 ID와 일치하지 않는 메시지를 저장하거나 처리하지 않는다. `Network.responseReceived`가 명령 완료 응답 전에 들어오면 버려진다. `goto()`가 끝난 뒤 `eval()`을 기다리는 동안 들어와도 버려진다.

`goto()`는 첫 `Page.loadEventFired`에서 대기를 끝내며 해당 탐색의 loader와 연결하지 않는다. 이후 고정 대기 중에 도착한 이벤트를 별도로 처리하지 않는다. 오래된 load 이벤트와 늦은 응답을 넣은 재현에서도 HTML은 읽지만 상태는 잃는다.

**유실 가능성은 코드와 재현으로 확정했다. 실제 실패에서 어느 메시지가 어느 시점에 버려졌는지는 원시 이벤트가 없어 미확인이다.**

**B. 오류 사유 유실 — 같은 `Tab.goto`.**

`Page.navigate` 결과의 `errorText`를 반영하지 않는다. 수신 타임아웃도 `except Exception: pass`로 지나간다. 따라서 `error=null`은 네트워크 오류가 없었다는 증거가 아니다. 이 두 결함도 오프라인에서 재현했지만 실제 실패의 직접 원인이라고 확정하지 않는다.

**C. 제한 화면 미인식 — [cpk_session.py](cpk_session.py)의 `is_challenge` 및 `classify_outcome`.**

현재 HTML 검사는 `sec-if-cpt-container`, `sec-bc-tile` 두 표식만 본다. 이번 `error403` 템플릿에는 둘 다 없다. `classify_outcome(None, 실제_실패_HTML, 0)`은 `load_error`를 반환한다. 이것은 원본 증거로 직접 재현한 오판이다.

**D. 중단 누락과 추가 요청 — `maybe_pause_on_block` 및 기본 `search`.**

공통 중단은 `challenge` 또는 HTTP 403/429에 반응한다. C의 오판은 여기에 해당하지 않아 중단이 걸리지 않는다. 기본 `search()`는 `load_error` 뒤에도 재시도하고 자동완성을 호출한다.

실제 `Tab.call/goto/eval`에 가짜 CDP 전송만 붙인 기본 경로 재현 결과: 홈 1회 + 검색 3회 + 자동완성 1회, 중단 없음. **이 5회는 오프라인 재현치이며 실제 쿠팡 접속 횟수가 아니다.** 실제 단건 테스트가 1회로 끝난 것은 실행 도구가 재시도·실패 후 자동완성을 제한했기 때문이다.

## 4. 재현 결과와 모의 테스트의 경계

[재현 스크립트](tests/audit_live_failure_20260917.py), [실행 결과](state/manual-tests/2026-09-17-atomic-audit.jsonl).

| 확인 항목 | 결과 |
|---|---|
| 명령 응답 뒤에 오는 HTTP 403을 기록 | 통과: 403 |
| 명령 응답 전에 오는 HTTP 403을 보존 | 위반: null |
| 늦게 도착해 DOM 조회 도중 읽힌 HTTP 403을 보존 | 위반: HTML은 확보, 상태 null |
| 탐색 명령의 오류 문자열 보존 | 위반: null |
| 수신 타임아웃 사유 보존 | 위반: 오류 기록 없음 |
| 실제 제한 HTML의 상품 파싱 | 통과: 상품 0개 |
| 상태 미기록이어도 명백한 제한 HTML이면 중단 | 위반: load_error, 중단 없음 |
| 기본 경로에서 제한 판정 후 추가 자동완성 억제 | 위반: 재시도와 자동완성 진행 |

8개 기대 동작 중 대조군 2개 통과, 6개 위반 재현. 서로 연관된 결함을 여러 각도에서 검증한 것이므로 독립된 근본 원인 6개라는 뜻은 아니다. 종료 코드 1은 미수정 결함을 표시한다.

이전 100개 모의 테스트는 `Tab.goto` 자체를 가짜로 교체하고 상태·상품 수를 이미 채워 주었다. 순차 처리·저장·예산은 확인했지만, 실제 CDP 이벤트 수신·상태 추출은 검증 대상에서 빠졌다. 따라서 그 통과 결과와 이번 실패는 모순되지 않는다.

## 5. 사용자 제안: UI 검색 후 응답 수집

검토할 가치가 있다. 현재 검색은 실제 Chrome을 쓰지만 `Page.navigate(검색_URL)`로 바로 이동한다. 제안은 홈 화면의 검색창 입력·제출을 거쳐 그 브라우저가 받은 결과를 저장하는 경로다. 자동완성은 현재 Python HTTP 호출이지만 UI에서 발생한 응답을 재사용하는 방향을 검토할 수 있다.

제안 흐름:

1. 브라우저 조작 전에 요청·응답 기록을 시작한다.
2. 공통 중단·예산 확인 후 검색창에 키워드 하나를 입력하고 화면의 검색 동작을 실행한다.
3. 이 동작에 연결된 문서 및 XHR/fetch 응답을 식별한다. 명령 응답을 기다리는 중에도 이벤트를 보존한다.
4. 브라우저가 이미 받은 응답 본문을 수집하고, 렌더링된 DOM이 필요한 항목은 DOM에서 읽는다. 캡처한 요청을 Python으로 다시 보내는 구조로 만들지 않는다.
5. 상품·연관검색어·자동완성의 결과와 출처를 저장한다. 제한 화면이면 즉시 공통 중단을 걸고 다음 키워드를 시작하지 않는다.

검색창 입력은 자동완성 요청을 여러 번 만들 수 있다. UI 동작 1회와 HTTP 요청 1회를 같은 것으로 세면 안 된다. 가능하면 한 번에 입력하고, 검색 문서·자동완성·그 밖의 리소스를 구분해 실제 발생량을 관측해야 한다. 기존 100개/201회 추정치를 새 UI 경로에 그대로 적용하지 않는다.

UI 검색도 내부적으로 서버 요청을 보낸다. 세션 및 화면 흐름이 달라져 결과가 달라질 가능성은 있지만, 이번 접근 제한을 해결하거나 100개 완주를 보장한다는 증거는 아직 없다. 홈 방문 유무·입력 방식·프로필·간격을 한꺼번에 바꾸면 어떤 차이가 효과를 냈는지도 알 수 없다.

## 6. 다음 작업의 순서와 완료 기준

1. **응답 기록:** 명령 응답과 이벤트를 모두 보존하고, request/frame/loader를 기준으로 해당 탐색에 귀속한다. 탐색 오류·타임아웃도 구분해 저장한다. 위 이벤트 순서 재현을 통과해야 한다.
2. **실패 판정:** 실제 제한 HTML을 별도 결과로 판정하고 HTTP 상태 미확인과 양립시킨다. 문구만 보고 `http_status=403`을 만들어 넣지 않는다. 해당 결과에서 재시도·자동완성이 더 나가지 않는지도 검증한다.
3. **UI 경로 검증:** 로컬 시험 페이지에서 입력→제출→요청/응답 연결→저장 흐름을 확인한다. 그다음 동일 프로필·공통 제어를 쓰는 제한된 실접속으로 UI 경로의 결과를 관측한다.

접근 제한의 원인(IP 평판, 세션 상태, URL 직접 이동, 시점 등)과 유료 프록시의 필요성은 아직 미결정이다. 다음 실험을 해석할 수 있도록 관측·판정을 먼저 신뢰할 수 있게 만드는 것이 이번 분석에서 도출한 우선순위다.

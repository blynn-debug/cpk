# 87204b9 후속 검토 — 요청 제어와 저장 보장

검토일: 2026-09-16. 대상: `87204b937e79fe0a1ef231b4357043ae10dfda07`.

## 결론

수정은 반영됐고 기존 오프라인 51건도 통과한다. 그러나 **모든 경로에서 중단 중 새 요청 0건**과 **동시 상태 갱신 시 카운터 보존**은 아직 충족하지 않는다. 프록시 도입 판단보다 아래 요청 제어 보완이 먼저다.

## 검증 범위

- 로컬과 맥미니의 소스 5개 및 테스트 파일 2개의 SHA-256 일치를 확인했다.
- 맥미니 Python으로 기존 유닛 32건, 회귀 19건을 실행했다. 실패·오류·skip 모두 0건이다.
- 추가 시나리오는 임시 상태/데이터 폴더, 가짜 CDP 탭, 가짜 HTTP 응답으로 실행했다. 실제 HTTP 클라이언트·소켓 연결은 차단하고, Chrome 시작/종료 및 알림도 막았다. 운영 상태 파일은 변경하지 않았다.
- 아래 숫자는 모의 호출 수다. 실제 쿠팡 접속 수나 IP 회복 관측치가 아니다.

## 수정 확인

| 시나리오 | 결과 |
|---|---|
| 이미 중단된 상태에서 기본 browser search와 공용 자동완성 호출 | 페이지 이동 0, HTTP 호출 0, 집계 0 |
| 예산 1에서 관문 진입 2회 | 두 번째는 거절, 집계는 1 유지 |
| 검색 잠금/간격 대기 도중 중단 설정 | 대기 뒤 거절, 집계 0 |
| 동일 파일에 작성자 8명이 동시에 atomic_write | 전부 완료, 최종 JSON 정상, 잔여 tmp 0 |

## P1 — 남은 제어·저장 결함

### 1. 관문 밖 요청 경로가 남아 있다

- `cpk_search.py:80`의 구형 검색은 `cs.search_html`과 저수준 `cs.autocomplete`를 직접 호출한다. 중단 상태에서 검색 1건을 실행하자 모의 HTTP 호출 **2건**, 공통 집계 **0건**이었다.
- `cpk_browser.py:199`의 `harvest`는 홈과 검색 재시도를 관문 없이 이동한다. 중단 상태에서 지속 403 응답을 주면 페이지 이동 **4건**, 공통 집계 **0건**이었다. 수동 재발급과 통합 테스트에서 이 경로를 호출한다.
- `cpk_browser.py:248`의 홈 워밍업은 중단 여부만 확인하고 예산·공통 간격·집계를 적용하지 않는다. 예산을 0으로 설정한 browser search는 최종 `budget`을 반환하면서도 홈을 **1회** 방문했다.
- `cpk_keepalive.py`의 Python 모드 역시 refresh/canary/저수준 autocomplete를 직접 호출한다(정적 확인).

수정 기준: 실제 전송을 하는 최소 단위에서 관문을 보장하고 상위의 중복 관문은 제거한다. 남겨 둘 구형 CLI·재발급·워밍업도 적용 대상이다. 폐기할 경로라면 호출 불가능하게 닫는다. 워밍업을 끄지 않은 기본 진입점으로 중단/예산 0 테스트를 해야 한다.

### 2. 온디맨드 차단이 공통 중단으로 전파되지 않는다

`cpk_browser.search`와 내부 fetch에는 차단 결과로 `cs.pause`를 호출하는 코드가 없다. 지속 403 응답을 주자 **검색 이동 3회 + 자동완성 HTTP 1회** 뒤 `http_error`를 반환했고 공통 중단은 설정되지 않았다. 다음 온디맨드 요청을 막을 근거가 생기지 않는다.

헬스체크는 실패 횟수 2부터 중단한다. 첫 실행도 **검색 이동 3회 + 자동완성 1회**를 거쳐 fails=1, 중단 없음이었다. 알림 임계치와 접근 중단 정책을 같은 값으로 취급하지 않아야 한다. 명확한 차단 후 즉시 중단할지, 제한된 재시도를 허용할지 공통 정책에서 명시해야 한다.

수정 기준: 실패 판정→재시도 허용/금지→공통 중단 설정을 공용 경로가 처리한다. 차단 결과를 받은 뒤 자동완성이나 다른 대기 요청이 먼저 시작하지 않도록 순서를 검증한다. 모든 HTTP 오류를 차단으로 단정하지 말고 403/429·명확한 챌린지와 기타 오류를 구분한다.

### 3. control.lock 대기 중 걸린 중단은 여전히 놓친다

`cpk_session.request_gate`는 검색 대기 후 `paused_remaining()`을 읽고, 별도로 `note_request()`의 control.lock을 기다린다. `note_request()`는 최신 상태를 다시 읽지만 중단 여부를 확인하지 않는다.

실제 파일 잠금과 스레드로 다음 순서를 재현했다.

1. control.lock을 다른 작업이 보유한다.
2. 요청 작업이 중단 없음 확인을 마치고 집계 잠금을 기다린다.
3. 잠금 보유 작업이 공통 중단을 기록하고 잠금을 해제한다.
4. 대기 요청이 중단 상태에서 집계되고 관문 본문에 진입한다.

결과: `admitted_while_paused=[true]`, 집계 1. 검색 잠금 대기 중 중단을 확인하는 기존 수정만으로는 이 순서를 막지 못한다.

수정 기준: 최신 중단 확인·날짜 갱신·예산 확인·허용된 요청 집계를 같은 control.lock 구간에서 수행한다. 이미 허가된 진행 중 요청과 중단 후 신규 허가를 구분하고, 실패 후 중단을 설정하기 전에 다음 요청이 허가되는 틈도 닫는다.

### 4. 고유 tmp는 상태의 읽기→수정→쓰기 충돌을 해결하지 않는다

고유 tmp 교체 자체는 동시 쓰기 검증을 통과했다. 그러나 `cpk_keepalive.py:58` 이후 health 읽기·runs/fails 증가·저장에는 전체 구간 잠금이 없다.

헬스체크 두 실행이 같은 이전 상태를 읽도록 동기화한 뒤 각각 실패를 저장했다. 두 실행이 끝났지만 **runs=1, fails=1**만 남아 실패 임계치 2에 따른 공통 중단도 설정되지 않았다.

수집기도 `cpk_collect.run`이 collect.run.lock 획득 **전에** 상태를 읽는다. 선행 실행이 그 사이 완료하면 잠금을 얻은 후에도 오래된 상태를 갱신할 수 있다(정적 확인).

수정 기준: health 갱신은 전용 잠금 안에서 최신 상태를 읽고 증가·저장한다. collect 상태는 실행 잠금 획득 뒤 읽는다. 단순 JSON 유효성 외에 동시 N회 갱신 후 카운터 N회 증가를 검증한다.

## 추가 확인 사항

### 자동완성 HTTP 실패가 정상 빈 결과로 표시됨

`cpk_session.autocomplete`는 HTTP 비정상 상태·JSON 파싱 오류에 빈 리스트를 반환한다. 상위 헬퍼는 예외가 없으면 성공으로 간주한다. 세 단어 검색어에 지속 HTTP 403을 주자 축약 호출까지 **3회** 보내고 `ok=True`를 반환했다.

HTTP 상태·응답 형식 오류를 보존하고, 정상 빈 결과에만 축약 후보를 시도해야 한다. 수집 결과와 depth 확장도 items만 추출하며 실패 정보를 버리는 부분을 함께 확인한다.

### 전날 예산 소진이 다음 날 수집을 막음

`cpk_collect.py:200`의 사전 예산 검사는 control.day를 확인하지 않는다. 날짜가 이전이고 counts가 예산만큼 있는 상태에서 run은 **return 7**로 끝났으며 날짜 갱신 관문에 도달하지 않았다. 다른 경로가 관문을 먼저 통과할 때까지 이런 상태가 유지될 수 있다. `--status`의 counts_today도 날짜 확인이 필요하다.

### 통합 테스트가 같은 IP의 공통 제어 상태를 공유하지 않음

`tests/test_integration.py`의 전용 포트·프로필은 운영 Chrome 프로세스 격리에는 도움이 된다. 그러나 테스트마다 CPK 상태 HOME을 바꾸므로 control.json과 요청 잠금도 분리된다. 테스트 재발급은 관문 밖 harvest도 사용한다(정적 확인, 온라인 실행하지 않음).

동일한 외부 IP로 실제 요청하는 통합 테스트라면 운영 중단·예산을 존중할 별도 공통 제어 경로가 필요하다. 데이터/쿠키 격리와 요청 허가 상태의 공유 범위를 따로 정의해야 한다.

## 다음 완료 기준

1. 모든 공개 진입점에 대해 중단·예산 소진 상태에서 HTTP 전송/페이지 이동 시도 0건.
2. 명확한 차단 뒤 공통 정책에 따라 수집·헬스·온디맨드·자동완성·재발급의 다음 요청이 중단됨.
3. 검색 잠금 및 제어 잠금 대기 중 중단, 자정 날짜 변경, 예산 경쟁을 회귀 테스트에 포함.
4. 동시 상태 갱신에서 카운터 보존. 고유 tmp 충돌 방지와 별도 검증.
5. HTTP 오류·형식 오류·정상 빈 결과를 구분하고 기존 요청 결과를 수동 관측. 그 근거로 직접 연결의 가용성과 프록시 필요성을 판단.
